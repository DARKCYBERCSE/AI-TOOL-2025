# Core Framework
streamlit==1.32.2

# Google AI (Gemini API)
google-generativeai==0.5.0

# Text-to-Speech
gtts==2.5.0

# Translation (Fixed version that works on cloud)
googletrans==3.1.0a0

# Image Processing
Pillow==10.2.0
opencv-python-headless==4.9.0.80

# Audio & Speech (pyttsx3 as fallback; pyaudio not supported on cloud)
# Removed: pyaudio, speechrecognition (not supported on Streamlit Cloud)

# PDF Export
fpdf2==2.7.4  # Updated fork of fpdf (better UTF-8 support)

# Word Document Export
python-docx==0.8.11

# QR Code Generation
qrcode[pil]==7.4.2

# Optional: For enhanced async handling (if needed)
asyncio==3.4.3

# Optional: Numpy (often needed by OpenCV and PIL)
numpy==1.24.4
